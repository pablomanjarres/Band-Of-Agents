{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "echo \"root install skipped; web deps installed in buildCommand\"",
  "buildCommand": "cd web && pnpm install --frozen-lockfile && pnpm run build",
  "outputDirectory": "web/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://band-backend-1068570846548.us-east1.run.app/api/:path*" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
